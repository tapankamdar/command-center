---
description: Run your morning brief — inbox, calendar, meeting notes, and carried-forward tasks
argument-hint: [optional focus area or time window]
---

Load the `morning-brief` skill and follow its workflow.

If the user provided arguments, treat $ARGUMENTS as context to focus or adjust the brief (e.g. a topic to weight toward, or a longer lookback window) and apply it during the fetch and synthesis steps.

If no arguments were provided, run the full brief immediately with no modifications.
